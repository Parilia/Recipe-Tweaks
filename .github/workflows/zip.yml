name: Zip repository
on: [workflow_dispatch]

jobs:
  zip:
    name: Zip
    runs-on: ubuntu-latest
    steps:
      - name: Zip Folder
        run: |
          cd ../ 
          rm -rf RecipeTweaks.zip
          git archive -o RecipeTweaks.zip HEAD 

